import { styled } from '@mui/system';

const Messenger = () => {
  return <Wrapper></Wrapper>;
};

const Wrapper = styled('div')({
  flexGrow: 1,
  display: 'flex',
  marginTop: '48px ',
  background: '#36393f',
});

export default Messenger;
